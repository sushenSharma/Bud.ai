import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BUD.ai - Your AI-Powered Cannabis Companion",
  description: "Discover personalized cannabis recommendations with BUD.ai - your intelligent guide to finding the perfect strain for your needs.",
  keywords: "cannabis, AI, recommendations, strain finder, cannabis app",
  openGraph: {
    title: "BUD.ai - Your AI-Powered Cannabis Companion",
    description: "Discover personalized cannabis recommendations with BUD.ai",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
